from setuptools import setup, find_packages

setup(
    name="src",  # Name of the package
    version="0.1",
    packages=find_packages(),  # Automatically finds all packages in 'src/'
    install_requires=[],  # Add dependencies here if needed
)